#!/usr/bin/env bash
# TODO: Testnet deploy script — not yet implemented.
# Planned steps:
#   1. cargo build -p tipjar --target wasm32v1-none --release
#   2. stellar contract deploy --wasm target/wasm32v1-none/release/tipjar.wasm ...
#   3. stellar contract invoke -- init --token <TOKEN_ADDRESS>
echo "Deploy script not yet implemented." && exit 1
